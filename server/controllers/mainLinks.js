const mainGet = (req, res) => {
  res.status(200).send(`<h1>hi full-stack</h1> <style> body {background-color: #1e1e1e;} h1 { color: #007acc;
  font-weight: bold; text-align: center; font-size: 35px; font-family: system-ui; text-transform: uppercase; <style>
  `)
};

const projectsPage = 0;

module.exports = {
  mainGet
}
